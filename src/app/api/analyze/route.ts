import{NextRequest,NextResponse}from"next/server";import{buildEvidenceJournals,DHET_EDITION}from"@/lib/kagua/evidence";import{applyEligibility}from"@/lib/kagua/eligibility";import{enrichWithLlm}from"@/lib/kagua/llm";import{rankJournals}from"@/lib/kagua/scoring";import type{AnalysisRequest,AnalysisResponse,EligibilityPolicy}from"@/lib/kagua/types";
export const runtime="nodejs";export const dynamic="force-dynamic";export const maxDuration=60;
export async function POST(req:NextRequest){try{const body=await req.json()as AnalysisRequest;if(!body.title||!body.abstract)return NextResponse.json({error:"A manuscript title and abstract are required."},{status:400});if(body.title.length>1000||body.abstract.length>30000)return NextResponse.json({error:"Manuscript input is too long."},{status:400});if((body.customJournalList?.entries.length||0)>10000)return NextResponse.json({error:"University journal list exceeds the 10,000-row safety limit."},{status:400});const ev=await buildEvidenceJournals(body),ranked=rankJournals(ev.journals,body),eligible=applyEligibility(ranked,body),top=eligible.slice(0,body.batchSize||5),policy:EligibilityPolicy=body.eligibilityPolicy||(body.dhetOnly===false?"all":"dhet");if(!top.length)return NextResponse.json({error:`No unseen journals passed scope and the selected ${policy.replaceAll("_"," ")} eligibility policy in the current live evidence pool.`,exhausted:true},{status:422});const fallback:AnalysisResponse={fingerprint:{field:body.field||"Inferred from manuscript evidence",keywords:[],noveltyClaim:"Evaluate against the target journal's recent literature.",methodSignal:"Review manuscript methods before submission."},journals:top,editorialBoard:{verdict:"Kagua ranked only journals passing the scientific/editorial scope threshold and the selected eligibility policy. Human researcher-supervisor consensus remains required.",strengths:["Live evidence discovery","ISSN-first DHET resolution","Explainable KTS/KPOS","Human-governed final selection"],concerns:["Unknown licensed metrics remain unverified"],actions:["Review the five targets","Inspect the Trilemma node ledger","Record researcher and supervisor decisions","Request five more if no consensus"]},evidenceNote:`Live sources: ${ev.sources.join(", ")}. DHET edition: ${DHET_EDITION}. Missing commercial metrics are not fabricated.`,evidenceRun:{generatedAt:new Date().toISOString(),candidateSources:ev.sources,dhetEdition:DHET_EDITION,liveEvidence:true},eligibilityRun:{policy,customListName:body.customJournalList?.name,customListEntries:body.customJournalList?.entries.length||0,preFilter:ranked.length,eligiblePool:eligible.length},llmModeUsed:"none",decisionRun:{batchSize:body.batchSize||5,returned:top.length,excludedCount:body.excludeJournalIds?.length||0,hasMore:eligible.length>top.length,eligiblePoolSize:eligible.length}};return NextResponse.json(await enrichWithLlm(body,fallback))}catch(error){console.error(error);return NextResponse.json({error:error instanceof Error?error.message:"Analysis failed."},{status:502})}}
