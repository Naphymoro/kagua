"use client";import{useEffect,useState}from"react";import Link from"next/link";
type Row={title:string;publisher?:string|null;issns:string[];indices:string[];quartile?:string|null;impactFactor?:number|null;impactFactorYear?:number|null;source:string;edition:string;sourceUrl:string;retrievedAt:string;matchedBy:string};
export default function Registry(){const[q,setQ]=useState(""),[quartiles,setQuartiles]=useState(""),[min,setMin]=useState(""),[max,setMax]=useState(""),[verified,setVerified]=useState(false),[rows,setRows]=useState<Row[]>([]),[total,setTotal]=useState(0),[busy,setBusy]=useState(false);async function load(){setBusy(true);const p=new URLSearchParams({limit:"250"});if(q)p.set("q",q);if(quartiles)p.set("quartiles",quartiles);if(min)p.set("jifMin",min);if(max)p.set("jifMax",max);if(verified)p.set("verifiedJif","true");const r=await fetch(`/api/registry?${p}`),d=await r.json();setRows(d.rows||[]);setTotal(d.total||0);setBusy(false)}useEffect(()=>{load()},[]);return <main className="shell"><header className="topbar"><div className="brand"><span className="brandMark">K</span><div><strong>Kagua</strong><small>Journal Registry</small></div></div><div className="topActions"><Link href="/" className="textButton">← Journal Hunter</Link><Link href="/manual" className="textButton">Manual</Link></div></header><section className="hero"><div className="eyebrow">DHET JOURNAL EXPLORER</div><h1>Browse the evidence <em>before you hunt.</em></h1><p>Explore the canonical DHET registry independently of a manuscript. Metrics are shown only when explicitly present in the source evidence.</p></section><section className="card explorer"><div className="rankHeader"><div><h2>Journal Registry</h2><small>{total.toLocaleString()} journals match the current filters · showing {rows.length}</small></div><a className="secondaryButton" href={rows[0]?.sourceUrl||"#"} target="_blank" rel="noreferrer">Open DHET source ↗</a></div><div className="grid2"><label>Journal, ISSN, publisher or index<input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search registry"/></label><label>Quartile<select value={quartiles} onChange={e=>setQuartiles(e.target.value)}><option value="">Any quartile</option><option value="Q1">Q1</option><option value="Q2">Q2</option><option value="Q3">Q3</option><option value="Q4">Q4</option><option value="Q1,Q2">Q1–Q2</option><option value="Q2,Q3">Q2–Q3</option><option value="Q3,Q4">Q3–Q4</option><option value="Q1,Q2,Q3">Q1–Q3</option><option value="Q2,Q3,Q4">Q2–Q4</option></select></label></div><div className="grid2"><label>Minimum JIF<input type="number" step="0.1" value={min} onChange={e=>setMin(e.target.value)}/></label><label>Maximum JIF<input type="number" step="0.1" value={max} onChange={e=>setMax(e.target.value)}/></label></div><label className="checkRow"><input type="checkbox" checked={verified} onChange={e=>setVerified(e.target.checked)}/><span>Show journals with explicit JIF evidence only</span></label><button className="primary" type="button" onClick={load}>{busy?"Filtering registry…":"Apply registry filters"}</button><div className="tableWrap"><table><thead><tr><th>Journal</th><th>ISSN/eISSN</th><th>DHET indices</th><th>Qx</th><th>JIF</th><th>JIF year</th><th>Publisher</th><th>Provenance</th></tr></thead><tbody>{rows.map((r,i)=><tr key={`${r.title}-${i}`}><td><b>{r.title}</b></td><td>{r.issns.join(", ")}</td><td>{r.indices.join(", ")}</td><td>{r.quartile||"Not provided by DHET source"}</td><td>{r.impactFactor??"Not provided by DHET source"}</td><td>{r.impactFactorYear??"—"}</td><td>{r.publisher||"—"}</td><td><details><summary>Source</summary><p><b>{r.source} {r.edition}</b></p><p>Matched by {r.matchedBy}</p><p>Retrieved {new Date(r.retrievedAt).toLocaleDateString()}</p><a href={r.sourceUrl} target="_blank" rel="noreferrer">Official source ↗</a></details></td></tr>)}</tbody></table></div></section></main>}
