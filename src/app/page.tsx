"use client";
import{FormEvent,useEffect,useMemo,useState}from"react";import Link from"next/link";import type{AnalysisResponse,JournalScore,LlmMode,LlmProviderId,MetricPreferences,Quartile}from"@/lib/kagua/types";
type Theme="light"|"dark";type Vote="undecided"|"approve"|"reject";type Decision={researcher:Vote;supervisor:Vote;note:string};
const providers=[{id:"openai" as LlmProviderId,label:"OpenAI",model:"gpt-5.6-sol",baseUrl:"https://api.openai.com/v1",keyUrl:"https://platform.openai.com/api-keys"},{id:"deepseek" as LlmProviderId,label:"DeepSeek",model:"deepseek-chat",baseUrl:"https://api.deepseek.com",keyUrl:"https://platform.deepseek.com/api_keys"}];const empty=():Decision=>({researcher:"undecided",supervisor:"undecided",note:""});const quartileOptions:{value:string;label:string;quartiles:Quartile[]}[]=[{value:"any",label:"Any quartile",quartiles:[]},{value:"Q1",label:"Q1 only",quartiles:["Q1"]},{value:"Q2",label:"Q2 only",quartiles:["Q2"]},{value:"Q3",label:"Q3 only",quartiles:["Q3"]},{value:"Q4",label:"Q4 only",quartiles:["Q4"]},{value:"Q1-Q2",label:"Q1–Q2",quartiles:["Q1","Q2"]},{value:"Q2-Q3",label:"Q2–Q3",quartiles:["Q2","Q3"]},{value:"Q3-Q4",label:"Q3–Q4",quartiles:["Q3","Q4"]},{value:"Q1-Q3",label:"Q1–Q3",quartiles:["Q1","Q2","Q3"]},{value:"Q2-Q4",label:"Q2–Q4",quartiles:["Q2","Q3","Q4"]},{value:"Q1-Q4",label:"Q1–Q4 (all verified quartiles)",quartiles:["Q1","Q2","Q3","Q4"]}];
export default function Home(){const[theme,setTheme]=useState<Theme>("light"),[title,setTitle]=useState(""),[abstract,setAbstract]=useState(""),[keywords,setKeywords]=useState(""),[budget,setBudget]=useState(2500),[days,setDays]=useState(45),[dhet,setDhet]=useState(true),[metrics,setMetrics]=useState<MetricPreferences>({apc:true,quartile:true,impactFactor:true}),[quartilePreset,setQuartilePreset]=useState("any"),[jifMin,setJifMin]=useState(""),[jifMax,setJifMax]=useState(""),[mode,setMode]=useState<LlmMode>("godmode"),[provider,setProvider]=useState<LlmProviderId>("openai"),[apiKey,setApiKey]=useState(""),[keyOpen,setKeyOpen]=useState(false),[model,setModel]=useState("gpt-5.6-sol"),[baseUrl,setBaseUrl]=useState("https://api.openai.com/v1"),[data,setData]=useState<AnalysisResponse|null>(null),[busy,setBusy]=useState(false),[error,setError]=useState(""),[seen,setSeen]=useState<string[]>([]),[batch,setBatch]=useState(1),[decisions,setDecisions]=useState<Record<string,Decision>>({}),[explore,setExplore]=useState(false),[sort,setSort]=useState<keyof JournalScore>("kpos");useEffect(()=>{document.documentElement.dataset.theme=theme},[theme]);const p=providers.find(x=>x.id===provider)||providers[0],kw=useMemo(()=>keywords.split(/[,;\n]/).map(x=>x.trim()).filter(Boolean),[keywords]),table=useMemo(()=>[...(data?.rankingExplorer||[])].sort((a,b)=>Number(b[sort]||0)-Number(a[sort]||0)),[data,sort]),selectedQuartiles=quartileOptions.find(x=>x.value===quartilePreset)?.quartiles||[];
function chooseProvider(id:LlmProviderId){setProvider(id);const x=providers.find(p=>p.id===id);if(x){setModel(x.model);setBaseUrl(x.baseUrl)}}function payload(exclude:string[],m:LlmMode){const llm=m==="provider"?{mode:m,provider,apiKey,model,baseUrl}:m==="godmode"&&apiKey?{mode:m,providers:[{id:provider,apiKey,model,baseUrl,enabled:true}]}:{mode:m};return{title,abstract,keywords:kw,budgetUsd:budget,desiredDays:days,dhetOnly:dhet,metricPreferences:metrics,constraintLocks:{dhet:true,apc:true,quartile:true,impactFactor:true,speed:false},quartileSelection:metrics.quartile?selectedQuartiles:[],impactFactorMin:metrics.impactFactor&&jifMin!==""?Number(jifMin):null,impactFactorMax:metrics.impactFactor&&jifMax!==""?Number(jifMax):null,excludeJournalIds:exclude,batchSize:5,explorerSize:50,llm}}
async function run(exclude:string[],m:LlmMode){const r=await fetch("/api/analyze",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload(exclude,m))}),x=await r.json();if(!r.ok)throw new Error(x.error||"Analysis failed");return x as AnalysisResponse}async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError("");try{const x=await run([],mode==="local"?"none":mode);setData(x);setSeen(x.journals.map(j=>j.id));setBatch(1);setExplore(false);setDecisions(Object.fromEntries(x.journals.map(j=>[j.id,empty()])))}catch(e){setError(e instanceof Error?e.message:"Analysis failed")}finally{setBusy(false)}}async function next(){if(!data)return;setBusy(true);try{const ex=[...new Set([...seen,...data.journals.map(j=>j.id)])],x=await run(ex,"none");setData(x);setSeen([...new Set([...ex,...x.journals.map(j=>j.id)])]);setBatch(n=>n+1)}catch(e){setError(e instanceof Error?e.message:"No more matching journals")}finally{setBusy(false)}}
return <main className="shell"><header className="topbar"><div className="brand"><span className="brandMark">K</span><div><strong>Kagua</strong><small>Scientific publication intelligence</small></div></div><div className="topActions"><Link href="/manual" className="textButton">Manual</Link><button type="button" className="iconButton" onClick={()=>setTheme(theme==="light"?"dark":"light")}>{theme==="light"?"☾ Dark":"☀ Light"}</button></div></header><section className="hero"><div className="eyebrow">SCOPE-FIRST JOURNAL INTELLIGENCE</div><h1>Find the journal your science <em>belongs in.</em></h1><p>Relevant publications provide scope evidence. Journals are then filtered, scored and ranked. The Top 5 remains the human decision room.</p></section><section className="workspace"><form className="card inputCard" onSubmit={submit}><div className="cardHead"><div><span className="step">01</span><h2>Manuscript signal</h2></div></div><label>Manuscript title<input required value={title} onChange={e=>setTitle(e.target.value)}/></label><label>Abstract<textarea required rows={7} value={abstract} onChange={e=>setAbstract(e.target.value)}/></label><label>Keywords<input value={keywords} onChange={e=>setKeywords(e.target.value)} placeholder="ammonia decomposition, ceria, single-atom catalyst, DFT"/></label><div className="guardrail"><b>Scope guardrail ≥60</b><span>Out-of-scope journals are removed before quality, APC or prestige can influence ranking.</span></div><label className="checkRow"><input type="checkbox" checked={dhet} onChange={e=>setDhet(e.target.checked)}/><span>DHET-recognised journals only</span></label><div className="divider"/><h2>Journal limits</h2><div className="metricToggles"><Metric label="APC" checked={metrics.apc} onChange={v=>setMetrics({...metrics,apc:v})}/><Metric label="Quartile" checked={metrics.quartile} onChange={v=>setMetrics({...metrics,quartile:v})}/><Metric label="Impact Factor" checked={metrics.impactFactor} onChange={v=>setMetrics({...metrics,impactFactor:v})}/></div>{metrics.apc&&<label>Maximum APC (USD)<input type="number" min="0" value={budget} onChange={e=>setBudget(Number(e.target.value))}/></label>}{metrics.quartile&&<label>Allowed quartile range<select value={quartilePreset} onChange={e=>setQuartilePreset(e.target.value)}>{quartileOptions.map(x=><option key={x.value} value={x.value}>{x.label}</option>)}</select><small className="filterHelp">Choose a single quartile or an inclusive range. A range such as Q1–Q3 accepts verified Q1, Q2 and Q3 journals. Unverified Qx is excluded when a restriction is active.</small></label>}{metrics.impactFactor&&<div className="grid2"><label>Minimum JIF<input type="number" step="0.1" value={jifMin} onChange={e=>setJifMin(e.target.value)}/></label><label>Maximum JIF<input type="number" step="0.1" value={jifMax} onChange={e=>setJifMax(e.target.value)}/></label></div>}<label>Desired first decision <strong>{days} days</strong><input type="range" min="7" max="120" value={days} onChange={e=>setDays(Number(e.target.value))}/></label><div className="divider"/><h2>Intelligence engine</h2><div className="segmented four">{(["godmode","local","provider","none"]as LlmMode[]).map(x=><button type="button" key={x} className={mode===x?"active":""} onClick={()=>setMode(x)}>{x}</button>)}</div>{mode!=="none"&&mode!=="local"&&<div className="modelPanel"><select value={provider} onChange={e=>chooseProvider(e.target.value as LlmProviderId)}>{providers.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select><div className="keyActions"><a className="secondaryButton" href={p.keyUrl} target="_blank" rel="noreferrer">Get API key ↗</a><button type="button" className="secondaryButton" onClick={()=>setKeyOpen(!keyOpen)}>Set API key</button></div>{keyOpen&&<input type="password" value={apiKey} onChange={e=>setApiKey(e.target.value)} placeholder="Session API key"/>}</div>}<button className="primary" disabled={busy}>{busy?"Running evidence pipeline…":"Hunt first 5 journals →"}</button>{error&&<p className="error">{error}</p>}</form><div className="results">{!data?<div className="empty card"><h3>Five defensible targets, with the full ranking behind them.</h3></div>:<><Funnel data={data}/>{data.llmRun&&<div className="card modelTrace"><div><span className="traceKicker">AI TRANSPARENCY</span><h3>{data.llmRun.selectedModel?`${data.llmRun.selectedProvider} · ${data.llmRun.selectedModel}`:"Deterministic scoring"}</h3><p>{data.llmRun.uncertaintyNote}</p></div></div>}<div className="rankHeader"><h2>Recommended now</h2><button type="button" className="secondaryButton" onClick={()=>setExplore(!explore)}>{explore?"Hide ranking explorer":`Explore ranking (${data.rankingExplorer.length})`}</button></div>{data.journals.map((j,i)=><Journal key={j.id} j={j} rank={i+1} d={decisions[j.id]||empty()} set={x=>setDecisions(s=>({...s,[j.id]:{...(s[j.id]||empty()),...x}}))}/>)}{explore&&<Explorer rows={table} sort={sort} setSort={setSort}/>}<div className="card decisionFooter"><h3>No consensus?</h3><button type="button" className="nextBatch" disabled={busy} onClick={next}>Show next 5 →</button></div></>}</div></section></main>}
function Funnel({data}:{data:AnalysisResponse}){const f=data.funnel,steps=[[f.relevantPublications,"Relevant publications found"],[f.candidateJournals,"Candidate journals"],[f.eligibilityPassed,"Eligibility passed"],[f.scopePassed,"Scope passed"],[f.constraintsPassed,"Meet your limits"],[f.ranked,"Ranked"],[f.recommended,"Recommended"]]as const;return <div className="card funnel"><div className="cardHead"><div><span className="step">AUDIT</span><h2>How Kagua narrowed the search</h2></div></div><div className="funnelSteps">{steps.map(([n,l],i)=><div key={l}><b>{n}</b><span>{l}</span>{i<steps.length-1&&<i>→</i>}</div>)}</div><p>Relevant publications are evidence of journal scope. Journals, not articles, are the entities ultimately ranked.</p></div>}
function Explorer({rows,sort,setSort}:{rows:JournalScore[];sort:keyof JournalScore;setSort:(x:keyof JournalScore)=>void}){return <div className="card explorer"><div className="rankHeader"><div><span className="traceKicker">AUDIT LAYER</span><h2>Top {rows.length} ranking explorer</h2></div><label>Sort by<select value={String(sort)} onChange={e=>setSort(e.target.value as keyof JournalScore)}><option value="kpos">KPOS</option><option value="trilemma">KTS</option><option value="fit">Scope fit</option><option value="quality">Quality</option><option value="impactFactor">JIF</option><option value="affordability">Affordability</option><option value="speed">Speed</option><option value="evidenceConfidence">Evidence confidence</option></select></label></div><div className="tableWrap"><table><thead><tr><th>Rank</th><th>Journal</th><th>Status</th><th>Fit</th><th>Qx</th><th>JIF</th><th>APC</th><th>Quality</th><th>KTS</th><th>KPOS</th><th>DHET</th><th>Evidence</th><th>Audit</th></tr></thead><tbody>{rows.map(j=><tr key={j.id}><td>{j.rank}</td><td><b>{j.name}</b></td><td>{j.batchLabel}</td><td>{j.fit}</td><td>{j.quartile}</td><td>{j.impactFactor??"—"}</td><td>{j.apcDisplay||"—"}</td><td>{j.quality}</td><td>{j.trilemma}</td><td><b>{j.kpos}</b></td><td>{j.dhet==="Recognised"?"Yes":"No"}</td><td>{Math.round(j.evidenceConfidence*100)}%</td><td><details><summary>Why?</summary><p>{j.rationale}</p><p>{j.matchedWorks} relevant publications supported scope evidence.</p></details></td></tr>)}</tbody></table></div></div>}
function Metric({label,checked,onChange}:{label:string;checked:boolean;onChange:(v:boolean)=>void}){return <button type="button" className={`metricToggle ${checked?"on":""}`} onClick={()=>onChange(!checked)}><b>{label}</b><i>{checked?"ON":"OFF"}</i></button>}
function Journal({j,rank,d,set}:{j:JournalScore;rank:number;d:Decision;set:(x:Partial<Decision>)=>void}){return <article className="journal card"><div className="rank">0{rank}</div><div className="journalMain"><div className="journalTitle"><div><h3>{j.name}</h3><p>{j.publisher} · {j.dhet}</p></div><span className="label good">{j.label}</span></div><div className="scopePass"><span>✓ Scope gate passed</span><b>{j.fit}/100</b></div><div className="scoreRow"><Score n="KPOS" v={j.kpos}/><Score n="KTS" v={j.trilemma}/><Score n="Scope" v={j.fit}/><Score n="Quality" v={j.quality}/></div><div className="facts"><span>Qx <b>{j.quartile}</b></span><span>JIF <b>{j.impactFactor??"Not verified"}</b></span><span>APC <b>{j.apcDisplay||"Unknown"}</b></span><span>Relevant publications <b>{j.matchedWorks}</b></span></div><p className="why">{j.rationale}</p><div className="voteGrid"><Vote label="Researcher" v={d.researcher} onChange={researcher=>set({researcher})}/><Vote label="Supervisor" v={d.supervisor} onChange={supervisor=>set({supervisor})}/></div></div></article>}
function Vote({label,v,onChange}:{label:string;v:Vote;onChange:(v:Vote)=>void}){return <div className="voteControl"><span>{label}</span><div><button type="button" className={`approve ${v==="approve"?"active":""}`} onClick={()=>onChange(v==="approve"?"undecided":"approve")}>✓ Suitable</button><button type="button" className={`reject ${v==="reject"?"active":""}`} onClick={()=>onChange(v==="reject"?"undecided":"reject")}>× Not suitable</button></div></div>}
function Score({n,v}:{n:string;v:number}){return <div className="score"><span>{n}</span><b>{v}</b><i><em style={{width:`${v}%`}}/></i></div>}
